/* // BUbble Sort : 9, 5, 13, 6, 2과 같은 숫자가 있을 시,
// 9와 5를 비교, 5와 13을 비교, 13과 6을 비교, 6과 2를 비교
// 오름차순으로 비교.

let bubble = ["9","5","13","6","2"]
bubble.sort();
console.log(bubble)
 */

/*  function sort(array){
     //let newArray = array;
     for(let i = array.length-1; i>0; i--){
        for(let k=0; k<i; k++){
            if(array[k] > array[k+1]){
                let tmp = array[k];
                array[k] = array[k+1];
                array[k+1] = tmp;
            }
        }
    }
}
    
let samples = [9, 5, 13, 6, 2];
console.log(sort(samples));
console.log(samples); */
